repos:
-   repo: https://github.com/commit-check/commit-check
    rev: v2.6.0
    hooks:
    -   id: check-message
        stages: [commit-msg]
    # -   id: check-branch
    # -   id: check-author-name
    # -   id: check-author-email